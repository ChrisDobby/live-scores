{
  "name": "@cleckheaton-ccc-live-scores/socket-disconnect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "package": "mkdir -p ../dist && rm -f ../dist/socket-disconnect.zip && npm run build && zip -r ../dist/socket-disconnect.zip lib/*.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.1145.0"
  }
}
