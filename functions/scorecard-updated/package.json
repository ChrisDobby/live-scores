{
  "name": "@cleckheaton-ccc-live-scores/scorecard-updated",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "package": "mkdir -p ../dist && rm -f ../dist/scorecard-updated.zip && npm run build && zip -r ../dist/scorecard-updated.zip lib/*.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.1145.0"
  }
}
