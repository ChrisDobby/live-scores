{
  "name": "teardown-processors",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "package": "mkdir -p ../dist && rm -f ../dist/teardown-processors.zip && npm run build && npm prune --production && zip -r ../dist/teardown-processors.zip lib/*.js node_modules/**/* -x *.test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.86.0"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
