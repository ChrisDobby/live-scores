{
  "name": "get-scorecard-urls",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "package": "mkdir -p ../dist && rm -f ../dist/get-scorecard-urls.zip && npm run build && npm prune --production && zip -r ../dist/get-scorecard-urls.zip lib/*.js node_modules/**/* -x *.test.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.85.0",
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer": "^13.6.0",
    "puppeteer-core": "^13.7.0"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
