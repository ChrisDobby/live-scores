{
  "name": "@cleckheaton-ccc-live-scores/get-scorecard-urls",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "package": "mkdir -p ../dist && rm -f ../dist/get-scorecard-urls.zip && npm run build && zip -r ../dist/get-scorecard-urls.zip lib/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer": "^13.6.0",
    "puppeteer-core": "^10.1.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.1145.0"
  }
}