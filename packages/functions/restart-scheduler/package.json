{
  "name": "@cleckheaton-ccc-live-scores/restart-scheduler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "types": "tsc --noEmit --incremental",
    "build": "rm -rf ./lib && esbuild ./src/index.ts --bundle --platform=node --outfile=lib/index.js --external:@aws-sdk/client-scheduler",
    "package": "mkdir -p ../dist && rm -f ../dist/restart-scheduler.zip && zip -r ../dist/restart-scheduler.zip lib/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-scheduler": "^3.363.0",
    "@aws-sdk/client-sns": "^3.131.0",
    "@cleckheaton-ccc-live-scores/schema": "*"
  }
}
