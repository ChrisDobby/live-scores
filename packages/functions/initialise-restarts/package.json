{
  "name": "@cleckheaton-ccc-live-scores/initialise-restarts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "types": "tsc --noEmit --incremental",
    "build": "rm -rf ./lib && esbuild ./src/index.ts --bundle --platform=node --outfile=lib/index.js --external:@aws-sdk/client-dynamodb --external:@aws-sdk/client-sqs",
    "package": "mkdir -p ../dist && rm -f ../dist/initialise-restarts.zip && zip -r ../dist/initialise-restarts.zip lib/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.85.0",
    "@aws-sdk/client-sqs": "^3.282.0",
    "@cleckheaton-ccc-live-scores/schema": "*"
  }
}
